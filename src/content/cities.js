// Intentionally empty. City-level landing pages were rolled back in favor of
// a fewer-but-deeper, original-content strategy. Keep this file as an empty
// export so any stale import keeps compiling during the transition.

export const cities = {};
export function getCityBySlug() { return null; }
